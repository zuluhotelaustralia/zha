use uo;

program textcmd_bcast( who, text)

SendSysMessage( who, " .t <msg> is NO longer being used. To fix your text, you should" );
SendSysMessage( who, "DELETE the file speech.mul in your UO3D directory." );

/*	if( !text )
		SendSysMessage( who, "Usage is : .t <msg>" );
	elseif (!who.dead)
		var color := who.backpack.color;
		if (!color || color==0) color:=0x3B2; endif
		PrintTextAbove( who, text, 0x3, color ); 
	endif
*/
endprogram