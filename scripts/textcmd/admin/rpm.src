use uo;
use os;
include "include/cmdlevel_check";

program textcmd_rpm( who )
	if (!checkStaff(who, "rpm", CMDLEVEL_ADMINISTRATOR))
		return 0;
	endif
    SendSysMessage( who, "RPM: " + system_rpm() );
endprogram