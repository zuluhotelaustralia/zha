use npc;
use uo;
include "include/attributes";


program missingai()

	var mob := Self();

	//MoveObjectToLocation(mob, 5288 , 1176 , 0, _DEFAULT_REALM, 0x40000000); //AutoConversion
	SetObjProperty(mob, "guardkill", 1);
	RevokePrivilege( mob, "invul" );
	mob.kill();

endprogram